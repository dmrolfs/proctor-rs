pub mod context;
pub mod policy {
    // use crate::elements::{make_item_context_policy, Policy, PolicySettings};
    // use crate::flink::decision::context::FlinkDecisionContext;
    // use crate::flink::MetricCatalog;
    // use oso::{Oso, PolarValue};
    //
    // type PolicyArgs = (MetricCatalog, FlinkDecisionContext, PolarValue::Variable);
    //
    // pub fn make_decision_policy(
    //  settings: &impl PolicySettings,
    // ) -> impl Policy<FlinkDecisionContext, PolicyArgs> {
    //     let init = |engine: &mut Oso| {
    //         engine.register_class(FlinkDecisionContext::get_polar_class())?;
    //         Ok(())
    //     };
    //     make_item_context_policy(
    //
    //     )
    // }
}
